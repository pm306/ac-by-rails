<% provide(:title, "Edit user") %>
<%= render 'layouts/flash_messages' %>
<h1>ユーザー情報の編集</h1>
<p>ユーザー名を変更する場合は、パスワードの入力は不要です。</p>
<p>パスワードを変更する場合は、現在のパスワードと変更後のパスワードを入力してください。</p>

<%= form_with(model: @user, 
              url: user_path(@user), 
              local: true,
              html: { class: "form-container" }) do |form| %>
  <ul>
    <li>
      <%= form.label :name, "ユーザー名", class:"label-user-edit" %>
      <%= form.text_field :name %>
    </li>
    <li>
      <%= form.label :current_password, "現在のパスワード", class:"label-user-edit" %>
      <%= form.password_field :current_password %>
    </li>
    <li>
      <%= form.label :password, "変更後のパスワード", class:"label-user-edit" %>
      <%= form.password_field :password %>
    </li>
    <li>
      <%= form.label :password_confirmation, "変更後のパスワード（確認）", class:"label-user-edit" %>
      <%= form.password_field :password_confirmation %>
    </li>
    <li>
    <%= form.submit "更新" %>
    </li>
  </ul>
<% end %>


<%= button_to "ユーザーを削除する(ハリボテ)", edit_user_path %>

<div>
  <%= back_link_to(root_path) %>
</div>